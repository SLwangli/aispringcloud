server:
  port: 8761
eureka:
  client:
    #是否获取其他Eureka Server的数据
    fetch-registry: false
    #是否将当前的Eureka Server服务器作为客户端注册
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
